/** JavaScript introduction **/

// This is a line comment
/*
* This is a block comment
*/

/** Numbers **/
const num = 10;
console.log(num);

/** Strings **/
const strNum = '20';
console.log(strNum);

/** Boolean **/
const foo = true;
const baz = false;
console.log(foo, baz);

/** Array **/
// An array can be of any data type: (you can mix them up to)
const list = ['apple', 'banana', 'pear'];
const secondList = [foo, baz, num];

/* use the [] bracket to specify the index of the array.
*  An array list starts at an index of 0! So index 1 is the second item!
*/
console.log(list[2]);

/** Objects **/
const car = {
  door: 'red',
  wheels: 4,
  speed: '240km/h'
};



